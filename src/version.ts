/**
 * Kept in sync with package.json. The User-Agent embeds it, so it must not
 * drift; the release checklist bumps both together.
 */
export const PKG_VERSION = "1.2.0";
export const REPO_URL = "https://github.com/smeet666/mcp-lyricscom";
